controller:
  image:
    registry: docker.io
    repository: zeroaninea/jenkins
    tag: custom
    pullPolicy: Always

  podTemplate:
    yaml: |
      apiVersion: v1
      kind: Pod
      spec:
        containers:
        - name: node-chrome
          image: cypress/included:latest
          tty: true
