import { InputValueBinderDirective } from './input-value-binder.directive';

describe('InputValueBinderDirective', () => {
  it('should create an instance', () => {
    const directive = new InputValueBinderDirective();
    expect(directive).toBeTruthy();
  });
});
